name: Determine responsibilities for the week

on:
  workflow_dispatch:

jobs:
  post-to-Discord:
    runs-on: ubuntu-latest
    steps:
      - name: Discord post action step
        id: discord
        uses: AAU-Dat/automate-team-responsibilities@v1.0
        with:
          team: '["Daniel", "Lars", "Raymond", "Elias", "Tobias", "Andreas", "Oliver"]'
          responsibilities: '[{"name": "Scrum master", "random": false}, {"name": "Cake", "random": false}, {"name": "Drinks", "random": false}]'
          discord-webhook: ${{ secrets.DISCORD_WEBHOOK }}
